<Page x:Class="HaloPixelToolBox.Installer.Views.Pages.Popups.AgreementDialogPopupPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:HaloPixelToolBox.Installer.Views.Pages.Popups"
      xmlns:viewmodel="clr-namespace:HaloPixelToolBox.Installer.ViewModel.Pages.Popups"
      xmlns:ctr="clr-namespace:HaloPixelToolBox.Installer.Views.Controls"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      d:DataContext="{d:DesignInstance Type=viewmodel:AgreementDialogPopupPageViewModel}"
      Title="协议同意书" Name="agreementDialogPopupPage">

    <Grid>
        <Grid.ColumnDefinitions>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="50"/>
        </Grid.RowDefinitions>
        <TextBlock Text="{Binding Title, ElementName=agreementDialogPopupPage}" Foreground="{DynamicResource MainColor}" FontSize="25" FontWeight="Bold" VerticalAlignment="Center" HorizontalAlignment="Center" Margin="0,5,0,0" TextAlignment="Center"/>
        <Border Grid.Row="1" CornerRadius="5" BorderThickness="0,2,0,2" BorderBrush="{DynamicResource MainColor}" Margin="0,5,0,0" Padding="5,5,0,5">
            <ctr:SmoothScrollViewer AnimateMilliseconds="800" ScrollDistanceMultiplier="0.5">
                <ctr:SmoothScrollViewer.Resources>
                    <Style TargetType="ScrollBar" BasedOn="{StaticResource ConsoleScrollBar}"/>
                </ctr:SmoothScrollViewer.Resources>
                <TextBlock Text="{Binding AgreementContent}" TextWrapping="Wrap"/>
            </ctr:SmoothScrollViewer>
        </Border>
        <ctr:RoundButton Grid.Row="3" VerticalAlignment="Center" HorizontalAlignment="Center" Command="{Binding ConfirmCommand}">
            <TextBlock Text="确定" Foreground="#9898e7" Margin="16,8"/>
        </ctr:RoundButton>
    </Grid>
</Page>
